{
  "name": "convex-oauth-preview-key-repro",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "description": "Minimal repro for OAuth-created Convex preview deploy keys failing claim_preview_deployment.",
  "scripts": {
    "repro:preview": "node repro-preview.ts",
    "repro:prod-control": "node repro-prod-control.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
